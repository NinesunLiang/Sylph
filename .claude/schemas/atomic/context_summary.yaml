# Schema: context_summary

# Version: v1

# Owner: harness-kit (shared)

# Description: 上下文收集摘要，被 4+ skill 复用

# Consumers: lx-tdd-spec, lx-browser-verify, lx-debug-spec, lx-root-cause-analysis


sources:
  type: array
  required: true
  items:
  type: object
  properties:
  type:
  type: enum
  values: [file, config, external_doc, code_pattern, test]
  path:
  type: string
  summary:
  type: string
  description: "信息来源列表"
key_findings:
  type: array
  required: true
  items:
  type: string
  description: "关键发现"
uncertainties:
  type: array
  required: false
  items:
  type: object
  properties:
  item:
  type: string
  confidence:
  type: enum
  values: [confirmed, medium, low]
  description: "不确定事项"

# 附录模板库（A-F）

>
> **调用时机**：`readFile(references/appendix-templates.md)` 在 Phase 2-E 写作附录章节时。
> P0 功能必须有附录 A（API 合约）+ 附录 B（配置模板）。附录 F 来自 Phase 1.5 Q 项，必须存在。

---

## 附录 A：API 合约

```
markdown
## 附录 A：API 合约
### A.1 {接口名}
- **方法**：POST / GET / PATCH / DELETE
- **路径**：`/api/{resource}`
- **请求体**： ```js
{ "{字段}": "{类型，说明}" }
```
- **响应体（成功）**： ```js
{ "{字段}": "{类型，说明}" }
```
- **错误码**：`4xx {描述}` / `5xx {描述}
```
---

## 附录 B：配置模板

```
markdown
## 附录 B：配置模板
```
yaml
#
{产品名} 配置示例（复制到实际配置文件并填写）
{配置项}: {默认值} # {说明}
```

---

## 附录 C：核心代码示例（≤30 行可验证）

```
markdown

## 附录 C：核心代码示例
```
{language}# {功能说明}{代码片段}
```
---

## 附录 D：数据模型定义

```
markdown

## 附录 D：数据模型定义
| 字段 | 类型 | 必填 | 说明|
|------|------|------|------|
|{字段名} | {类型} | 是/否 | {说明} |

```
---

## 附录 E：第三方依赖说明

```
markdown

## 附录 E：第三方依赖说明
| 依赖 | 版本 | 用途 | SLA/限制|
|------|------|------|---------|
|{名称} | {版本} | {用途} | {限制} |

```

---

## 附录 F：人工复核填写表（来自 Phase 1.5，必须存在）

```
markdown

## 附录 F：人工复核填写表
> 以下内容 AI 无法自行确定，需人工填写后方可启动开发。

### F.1 {Q 类别1}（对应 Q1-QN）
| 字段 | 值 | 获取方式|
|------|-----|--------|
|{字段名} | *___*填写___ | {从哪里获取} |

### F.N 启动检查清单
- [ ] F.1 {类别1} 全部填写
- [ ] F.2 {类别2} 全部填写
- [ ] 关键 ADR 已用户确认
- [ ] Phase 1.1 环境已就绪

```

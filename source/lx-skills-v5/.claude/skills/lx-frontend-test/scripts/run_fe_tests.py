#!/usr/bin/env python3

"""
# 前端测试生成 — 危险信号自检清单


在输出最终报告前，逐条自检。任何未通过项 → 回溯对应步骤修正。
## 测试准确性检查
- [ ] 是否为未导出/内部组件生成了不必要的测试？（应聚焦公开 API）
- [ ] 是否在不了解组件 props 类型的情况下编写了测试？（必须先 Read 类型定义）
- [ ] 是否假设了组件的渲染输出而未实际运行验证？（= 虚构测试）
- [ ] 是否使用了已废弃的测试 API（如 `enzyme`、`ReactDOM.render`）？（应使用 RTL）
- [ ] 是否在 E2E 测试中硬编码了环境特定的 URL/端口？（应使用配置）
## 证据完整性检查
- [ ] 每个生成的测试文件是否都实际执行了 `jest`/`vitest`/`playwright` 验证？
- [ ] 是否有测试被"跳过"执行验证而直接声称通过？
- [ ] 测试通过/失败数是否与实际命令输出一致？
- [ ] 覆盖率数值是否来自实际 `--coverage` 输出（非估算）？
## 测试质量检查
- [ ] 是否测试了实现细节而非用户行为？（如直接检查 state 而非 UI 输出）
- [ ] 是否过度使用 `getByTestId` 而忽略了语义查询（`getByRole`、`getByText`）？
- [ ] 是否在测试中使用了 `waitFor` 包裹不需要异步等待的断言？
- [ ] mock 是否过度？（mock 了被测组件自身的方法 = 测试无意义）
- [ ] E2E 测试是否依赖了特定数据库状态而非自包含 setup？
## 误报防护检查
- [ ] 是否为纯展示组件生成了交互测试？（无交互逻辑则不需要）
- [ ] 是否为第三方组件库的封装生成了测试其内部行为的用例？（应只测接口）
- [ ] 是否把 CSS-in-JS / Tailwind 类名作为断言目标？（脆弱断言）
- [ ] 是否为 `getServerSideProps` / `getStaticProps` 生成了 DOM 测试？（应为数据测试）
## 不确定性处理
- [ ] 对复杂异步流（如 SWR/React Query + Suspense）是否标记为"待确认：需人工验证时序"？
- [ ] 对动态路由组件是否确认了路由参数 mock 方式？
- [ ] 对使用 Context/Provider 的组件是否确认了 wrapper 需求？
- [ ] 是否以确定语气声称覆盖率达标但未实际运行 `--coverage`？（= 过度自信违规）
"""

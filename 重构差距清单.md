# 重构实施差距清单（1.md ~ 10.md vs 磁盘现状）

## 1.md — IntakeGate（任务入口与 L1/L2 分级）
| 文档要求 | 磁盘现状 | 状态 |
|----------|----------|------|
| IntakeGate 作为任务唯一入口 | carros_base.py cmd_init() 中有 intake_gate.py 调用（有条件），但文件不存在 | ❌ 缺 |
| 输出固定 JSON schema（decision/task_type/risk_level 等） | carros_base.py 有 IntakeGate 调用逻辑但 intake_gate.py 不存在 | ❌ 缺 |
| L1/L2 分级按任务风险，不按模型档位 | AGENTS.md/update.md 使用旧概念 BASE/ENHANCE 而非 L1/L2 | ⚠️ 混淆 |
| BLOCKED/ASK_USER 定义 | 无实现 | ❌ 缺 |

## 2.md — PlanBuilder（计划生成器）
| 文档要求 | 磁盘现状 | 状态 |
|----------|----------|------|
| L1 Plan A/B/C 三段式模板 | plan.md 由 carros_base.py `_write_default_plan()` 生成，但格式较简单 | ⚠️ 需对齐 |
| L2 Plan A/B/C/D/E 五段式 | 无 L2 模板 | ❌ 缺 |
| scope freeze 规则 | plan.md 已有冻结声明行 | ✅ 部分有 |
| verify 规则前缀 `command:`/`file:`/`assertion:`/`user:` | 无此格式 | ❌ 缺 |

## 3.md — PreActionGate（动作级安全门）
| 文档要求 | 磁盘现状 | 状态 |
|----------|----------|------|
| 9 种动作 × 4 种裁决 | 无此实现 | ❌ 缺 |
| permission_gate.py block remote/delete 等 | 存在 permission_gate.py | ✅ 已有 |

## 4.md — Executor Evidence Ledger
| 文档要求 | 磁盘现状 | 状态 |
|----------|----------|------|
| 7 种证据类型 + 4 级证据强度 | executor.md 当前是自由格式文本 | ❌ 需规范格式 |
| 追加式 ID 编号 evidence | 无 | ❌ 缺 |

## 5.md — VerifyGate（完成裁决门）
| 文档要求 | 磁盘现状 | 状态 |
|----------|----------|------|
| 输出 VERIFIED/WARN/BLOCKED/REJECTED | carros_base.py verify 只输出 VERIFIED | ⚠️ 缺 WARN/BLOCKED/REJECTED |
| 软完成语黑名单 | 无检查 | ❌ 缺 |

## 6.md — Context Engine（上下文水位/压缩）
| 文档要求 | 磁盘现状 | 状态 |
|----------|----------|------|
| BASE 用轮数 compact（15/20）| 无 | ❌ 缺 |
| ENHANCE 用水位 compact（70%/85%）| 无 | ❌ 缺 |
| session-handoff.md 格式 | 05 bench 测试中写过手动的 handoff | ⚠️ 需规范 |

## 7.md — Oracle / Meta-Oracle
| 文档要求 | 磁盘现状 | 状态 |
|----------|----------|------|
| L2 二阶复核 | oracle_gate.py 存在，但功能单薄 | ⚠️ stub |
| oracle-verdicts.md | 不存在 | ❌ 缺 |

## 8.md — Fallback Protocol
| 文档要求 | 磁盘现状 | 状态 |
|----------|----------|------|
| L2→L1 降级矩阵 | 无 fallback_matrix.py | ❌ 缺 |
| 熔断机制 | 无 | ❌ 缺 |

## 9.md — CLI Integration
| 文档要求 | 磁盘现状 | 状态 |
|----------|----------|------|
| CLI 观测接口 | 无 | ❌ 缺 |

## 10.md — Archive Engine
| 文档要求 | 磁盘现状 | 状态 |
|----------|----------|------|
| tombstone 写入 | carros_base.py archive 有 token.tombstone.json | ✅ 已有 |
| final-report | carros_base.py archive 有 _generate_final_report() | ✅ 已有 |
| Sovereign Verdict | 无 | ❌ 缺 |
